<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Job Portal", root_path, class: "navbar-brand" %>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Sing Out", destroy_user_session_path, class: "brand pull-right" %></li>
          <li><%= link_to "Welcome :  #{current_user.role}", organizations_path, class: "brand pull-right" %></li>
        <% else %>
          <li><%= link_to "Sing Up", new_user_registration_path, class: "brand pull-right" %></li>
          <li><%= link_to "Sing In", new_user_session_path , class: "brand pull-right" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<div class="flash-messages">
  <% flash.each do |type, message| %>
    <div class="alert <%= bootstrap_class_for(type) %> fade in">
      <button class="close" data-dismiss="alert">×</button>
      <%= message.html_safe %>
    </div>
    <% flash.clear %>
  <% end %>
</div>
